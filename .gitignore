node_modules/
data/*.db
data/*.db-wal
data/*.db-shm
*.log
.DS_Store
.env
.env.local

# app/public is the built UI (Vite output). It IS committed so that a plain
# `node server.mjs` after a clone or merge serves the current interface with
# no build step. Rebuild it with `npm run build` in app/ whenever you change
# anything under app/web/src, and commit the result.
# vite dev cache
app/web/.vite/
app/web/dist/

# source reference material — never ship
Freight Agreement - Signed.pdf
*.pdf
app/test/*.db
app/test/*.db-shm
app/test/*.db-wal
